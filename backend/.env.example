# Path to the trained Keras model file.
# Copy your model.h5 (trained in Google Colab or elsewhere) into the backend/
# directory and set this variable to its path.
MODEL_PATH=./model.h5

# SQLite database URL (default)
DATABASE_URL=sqlite:///./football.db
